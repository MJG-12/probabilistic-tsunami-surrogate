# Local data

Simulation inputs, processed arrays, split files and model checkpoints are not
committed to Git. See the repository README for the expected directory layout.

If historical run artifacts are recovered, record their checksums and
provenance before comparing them with report outputs.
